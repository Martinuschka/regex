# regex
regex tester - a CLI tool to test and understand regular expressions.

## usage
```bash
python regex_tester.py --pattern "\d{3}" --text "ABC123XYZ"
python regex_tester.py -p "\d{3}" -t "ABC123XYZ"
python regex_tester.py --interactive
```
